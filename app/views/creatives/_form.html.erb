<%= form_for([@creative.ad, @creative]) do |f| %>
  <% if @creative.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@creative.errors.count, "error") %> prohibited this creative from being saved:</h2>

      <ul>
      <% @creative.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :bid %><br>
    <%= f.text_field :bid %>
  </div>
  <div class="field">
    <%= f.label :adtext %><br>
    <%= f.text_area :adtext %>
  </div>
  <div class="field">
    <%= f.label :ad_id %><br>
    <%= f.text_field :ad_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
